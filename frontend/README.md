# Frontend (Vite + React + TypeScript + MUI)

Local setup (Windows PowerShell):

```powershell
cd frontend
npm install
npm run dev
```

- Dev server runs on `http://localhost:3000` by default.
- The example `Home` page calls the backend at `http://localhost:5000/api/hello/`.
